-- Private Test UI
-- Put this LocalScript in:
-- StarterPlayer > StarterPlayerScripts

local Players = game:GetService("Players")

local player = Players.LocalPlayer

--// SETTINGS
local Settings = {
    AutoBuyAllSeeds = false,
    AutoHarvestAll = false,
    AutoPickupAllFruit = false,
    AutoSellAll = false,

    BuySeeds = false,
    AutoSell = false,
    AutoBuyEgg = false
}

--// GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PrivateTestUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

-- Toggle Button
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 120, 0, 40)
ToggleButton.Position = UDim2.new(0, 20, 0.5, -20)
ToggleButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Text = "OPEN MENU"
ToggleButton.TextSize = 14
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Parent = ScreenGui

Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(0, 8)

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 280, 0, 400)
MainFrame.Position = UDim2.new(0.5, -140, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "PRIVATE TEST MENU"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

-- Scrolling area
local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(1, -20, 1, -65)
Scroll.Position = UDim2.new(0, 10, 0, 55)
Scroll.BackgroundTransparency = 1
Scroll.BorderSizePixel = 0
Scroll.ScrollBarThickness = 4
Scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
Scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
Scroll.Parent = MainFrame

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0, 8)
Layout.Parent = Scroll

--// CREATE TOGGLE
local function CreateToggle(text, settingName)

    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1, 0, 0, 42)
    Button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.Font = Enum.Font.Gotham
    Button.TextSize = 14
    Button.Parent = Scroll

    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 7)

    local function UpdateButton()
        local status = Settings[settingName]

        if status then
            Button.Text = text .. " : ON"
            Button.BackgroundColor3 = Color3.fromRGB(40, 140, 70)
        else
            Button.Text = text .. " : OFF"
            Button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
        end
    end

    Button.MouseButton1Click:Connect(function()
        Settings[settingName] = not Settings[settingName]
        UpdateButton()
    end)

    UpdateButton()

    return Button
end

-- MAIN OPTIONS
CreateToggle("Auto Buy All Seeds", "AutoBuyAllSeeds")
CreateToggle("Auto Harvest All", "AutoHarvestAll")
CreateToggle("Auto Pick Up All Fruit", "AutoPickupAllFruit")
CreateToggle("Auto Sell All", "AutoSellAll")

-- Section Label
local SelectiveLabel = Instance.new("TextLabel")
SelectiveLabel.Size = UDim2.new(1, 0, 0, 35)
SelectiveLabel.BackgroundTransparency = 1
SelectiveLabel.Text = "SELECTIVE OPTIONS"
SelectiveLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
SelectiveLabel.TextSize = 13
SelectiveLabel.Font = Enum.Font.GothamBold
SelectiveLabel.Parent = Scroll

-- SELECTIVE OPTIONS
CreateToggle("Buy Seeds", "BuySeeds")
CreateToggle("Auto Sell", "AutoSell")
CreateToggle("Auto Buy Egg", "AutoBuyEgg")

-- OPEN / CLOSE MENU
ToggleButton.MouseButton1Click:Connect(function()

    MainFrame.Visible = not MainFrame.Visible

    if MainFrame.Visible then
        ToggleButton.Text = "CLOSE MENU"
    else
        ToggleButton.Text = "OPEN MENU"
    end

end)

--------------------------------------------------
-- PRIVATE TEST FUNCTIONS
-- Connect these functions to YOUR game's systems
--------------------------------------------------

local function AutoBuyAllSeeds()
    print("Auto buying all seeds...")
    
    -- Add your game's seed purchasing logic here
end

local function AutoHarvestAll()
    print("Auto harvesting all crops...")

    -- Add your own harvesting system here
end

local function AutoPickupAllFruit()
    print("Picking up all fruit...")

    -- Add your own pickup system here
end

local function AutoSellAll()
    print("Selling all items...")

    -- Add your own selling system here
end

local function SelectiveBuySeeds()
    print("Selective seed buying enabled...")

    -- Add selected seed logic
end

local function SelectiveAutoSell()
    print("Selective auto sell enabled...")

    -- Add selected item selling logic
end

local function AutoBuyEgg()
    print("Auto buying egg...")

    -- Add your own egg purchasing logic
end

--------------------------------------------------
-- AUTOMATION LOOP
--------------------------------------------------

task.spawn(function()

    while task.wait(1) do

        if Settings.AutoBuyAllSeeds then
            AutoBuyAllSeeds()
        end

        if Settings.AutoHarvestAll then
            AutoHarvestAll()
        end

        if Settings.AutoPickupAllFruit then
            AutoPickupAllFruit()
        end

        if Settings.AutoSellAll then
            AutoSellAll()
        end

        if Settings.BuySeeds then
            SelectiveBuySeeds()
        end

        if Settings.AutoSell then
            SelectiveAutoSell()
        end

        if Settings.AutoBuyEgg then
            AutoBuyEgg()
        end

    end

end)
