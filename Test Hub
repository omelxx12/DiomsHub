--[[
    PERSONAL TEST GUI
    Place in:
    StarterPlayer > StarterPlayerScripts
]]

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local speedEvent = ReplicatedStorage:WaitForChild("SetTestWalkSpeed")

--------------------------------------------------
-- SETTINGS
--------------------------------------------------

local InfiniteJump = false
local SpeedEnabled = false

local NORMAL_SPEED = 16
local FAST_SPEED = 200

--------------------------------------------------
-- CREATE GUI
--------------------------------------------------

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PersonalTestGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.fromOffset(300, 260)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -130)
MainFrame.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 12)
Corner.Parent = MainFrame

-- Header
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 50)
Header.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
Header.BorderSizePixel = 0
Header.Parent = MainFrame

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 12)
HeaderCorner.Parent = Header

-- Fix bottom corners of header appearance
local HeaderBottom = Instance.new("Frame")
HeaderBottom.Size = UDim2.new(1, 0, 0, 15)
HeaderBottom.Position = UDim2.new(0, 0, 1, -15)
HeaderBottom.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
HeaderBottom.BorderSizePixel = 0
HeaderBottom.Parent = Header

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -60, 1, 0)
Title.Position = UDim2.fromOffset(15, 0)
Title.BackgroundTransparency = 1
Title.Text = "PERSONAL TEST GUI"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Parent = Header

-- Close Button
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.fromOffset(35, 35)
CloseButton.Position = UDim2.new(1, -43, 0.5, -17)
CloseButton.BackgroundColor3 = Color3.fromRGB(180, 65, 65)
CloseButton.Text = "×"
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 24
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.BorderSizePixel = 0
CloseButton.Parent = Header

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseButton

--------------------------------------------------
-- CONTENT
--------------------------------------------------

local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, -30, 1, -65)
Content.Position = UDim2.fromOffset(15, 55)
Content.BackgroundTransparency = 1
Content.Parent = MainFrame

--------------------------------------------------
-- HELPER FUNCTION FOR BUTTONS
--------------------------------------------------

local function createButton(name, position)
	local button = Instance.new("TextButton")

	button.Name = name
	button.Size = UDim2.new(1, 0, 0, 45)
	button.Position = position
	button.BackgroundColor3 = Color3.fromRGB(55, 55, 65)
	button.BorderSizePixel = 0

	button.Font = Enum.Font.Gotham
	button.TextSize = 15
	button.TextColor3 = Color3.fromRGB(255, 255, 255)

	button.Parent = Content

	local buttonCorner = Instance.new("UICorner")
	buttonCorner.CornerRadius = UDim.new(0, 8)
	buttonCorner.Parent = button

	return button
end

--------------------------------------------------
-- INFINITE JUMP BUTTON
--------------------------------------------------

local JumpButton = createButton(
	"InfiniteJump",
	UDim2.fromOffset(0, 0)
)

JumpButton.Text = "Infinite Jump     OFF"

--------------------------------------------------
-- WALKSPEED BUTTON
--------------------------------------------------

local SpeedButton = createButton(
	"WalkSpeed",
	UDim2.fromOffset(0, 55)
)

SpeedButton.Text = "WalkSpeed          OFF"

--------------------------------------------------
-- SPEED INPUT
--------------------------------------------------

local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(1, 0, 0, 40)
SpeedBox.Position = UDim2.fromOffset(0, 110)
SpeedBox.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
SpeedBox.BorderSizePixel = 0
SpeedBox.PlaceholderText = "Enter WalkSpeed (Example: 200)"
SpeedBox.Text = tostring(FAST_SPEED)
SpeedBox.Font = Enum.Font.Gotham
SpeedBox.TextSize = 14
SpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBox.PlaceholderColor3 = Color3.fromRGB(160, 160, 160)
SpeedBox.ClearTextOnFocus = false
SpeedBox.Parent = Content

local BoxCorner = Instance.new("UICorner")
BoxCorner.CornerRadius = UDim.new(0, 8)
BoxCorner.Parent = SpeedBox

--------------------------------------------------
-- OPEN GUI BUTTON
--------------------------------------------------

local OpenButton = Instance.new("TextButton")
OpenButton.Name = "OpenButton"
OpenButton.Size = UDim2.fromOffset(120, 42)
OpenButton.Position = UDim2.new(0, 20, 0.5, -21)
OpenButton.BackgroundColor3 = Color3.fromRGB(50, 120, 200)
OpenButton.BorderSizePixel = 0
OpenButton.Text = "OPEN TEST GUI"
OpenButton.Font = Enum.Font.GothamBold
OpenButton.TextSize = 13
OpenButton.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenButton.Visible = false
OpenButton.Parent = ScreenGui

local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(0, 8)
OpenCorner.Parent = OpenButton

--------------------------------------------------
-- DRAGGING SYSTEM
--------------------------------------------------

local dragging = false
local dragStart
local startPosition

Header.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPosition = MainFrame.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and
		(input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch) then

		local delta = input.Position - dragStart

		MainFrame.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)
	end
end)

--------------------------------------------------
-- INFINITE JUMP
--------------------------------------------------

JumpButton.MouseButton1Click:Connect(function()

	InfiniteJump = not InfiniteJump

	if InfiniteJump then
		JumpButton.Text = "Infinite Jump     ON"
		JumpButton.BackgroundColor3 = Color3.fromRGB(50, 140, 80)
	else
		JumpButton.Text = "Infinite Jump     OFF"
		JumpButton.BackgroundColor3 = Color3.fromRGB(55, 55, 65)
	end
end)

UserInputService.JumpRequest:Connect(function()

	if not InfiniteJump then
		return
	end

	local character = player.Character

	if character then

		local humanoid = character:FindFirstChildOfClass("Humanoid")

		if humanoid then
			humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
		end
	end
end)

--------------------------------------------------
-- WALKSPEED
--------------------------------------------------

SpeedButton.MouseButton1Click:Connect(function()

	SpeedEnabled = not SpeedEnabled

	if SpeedEnabled then

		local requestedSpeed = tonumber(SpeedBox.Text)

		if not requestedSpeed then
			requestedSpeed = FAST_SPEED
			SpeedBox.Text = tostring(requestedSpeed)
		end

		SpeedButton.Text = "WalkSpeed          ON (" .. requestedSpeed .. ")"
		SpeedButton.BackgroundColor3 = Color3.fromRGB(50, 140, 80)

		speedEvent:FireServer(requestedSpeed)

	else

		SpeedButton.Text = "WalkSpeed          OFF (16)"
		SpeedButton.BackgroundColor3 = Color3.fromRGB(55, 55, 65)

		speedEvent:FireServer(NORMAL_SPEED)
	end
end)

--------------------------------------------------
-- CLOSE / OPEN
--------------------------------------------------

CloseButton.MouseButton1Click:Connect(function()
	MainFrame.Visible = false
	OpenButton.Visible = true
end)

OpenButton.MouseButton1Click:Connect(function()
	MainFrame.Visible = true
	OpenButton.Visible = false
end)
